---
metadata:
  customer: home
  environment: production
  location: home
  os: linux
  services:
    - graylog
  tags: []
  groups:
    - home


ansible_host: ap01.test.local

password: !vault |
  $ANSIBLE_VAULT;1.1;AES256
  663736303132
